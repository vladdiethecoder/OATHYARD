# Production asset sources

Only source-approved production DCC/interchange files belong here. Candidate-only Rodin/procedural/generated outputs are not allowed in this lane.

Expected source types: `.blend`, `.usd`, `.usda`, `.usdc`, `.fbx`, or another ADR-approved source/interchange format with provenance and license metadata.
