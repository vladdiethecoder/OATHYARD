# Runtime asset output lane

Future cooked/native-renderer asset outputs belong here when generated from `assets_src/production/`. Runtime outputs are presentation-only unless separately promoted by a deterministic truth ADR.
